#include <stdio.h>
#define MAX 10

// Fonctions d'affichage et de saisie
void lire(int m[MAX][MAX], int ligne, int colonne) {
    for(int i=0; i<ligne; i++)
        for(int j=0; j<colonne; j++) {
            printf("Element [%d][%d] : ", i+1, j+1);
            scanf("%d", &m[i][j]);
        }
}

void afficher(int m[MAX][MAX], int ligne, int colonne) {
    for(int i=0; i<ligne; i++) {
        for(int j=0; j<colonne; j++) printf("%d\t", m[i][j]);
        printf("\n");
    }
}

// Fonctions d'operations
void addition(int m1[MAX][MAX], int m2[MAX][MAX], int res[MAX][MAX], int ligne, int colonne) {
    for(int i=0; i<ligne; i++)
        for(int j=0; j<colonne; j++) res[i][j] = m1[i][j] + m2[i][j];
}

void multiplication(int m1[MAX][MAX], int m2[MAX][MAX], int resultat[MAX][MAX], int ligne1, int colonne1, int colonne2) {
    for(int i=0; i<ligne1; i++)
        for(int j=0; j<colonne2; j++) {
            resultat[i][j] = 0;
            for(int k=0; k<c1; k++) res[i][j] += m1[i][k] * m2[k][j];
        }
}

void transposee(int m[MAX][MAX], int res[MAX][MAX], int l, int c) {
    for(int i=0; i<ligne; i++)
        for(int j=0; j<colonne; j++) resultat[j][i] = m[i][j];
}

int main() {
    int A[MAX][MAX], B[MAX][MAX], R[MAX][MAX];
    int ligne = 2, colonne = 2; // On fixe une taille de 2x2

    printf("--- Saisie de la Matrice A (2x2) ---\n");
    lire(A, ligne, colonne);
    printf("--- Saisie de la Matrice B (2x2) ---\n");
    lire(B, ligne, colonne);

    // 1. Addition
    addition(A, B, R, ligne, colonne);
    printf("\n> Resultat Addition :\n");
    afficher(R, ligne, colonne);

    // 2. Multiplication
    multiplication(A, B, R, ligne, colonne, colonne);
    printf("\n> Resultat Multiplication :\n");
    afficher(R, ligne, colonne);

    // 3. Transposee de A
    transposee(A, R, ligne colonne);
    printf("\n> Transposee de la Matrice A :\n");
    afficher(R, colonne, ligne);

    return 0;
}
